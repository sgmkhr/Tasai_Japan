<% chats.each do |chat| %>

  <% if chat.user_id == current_user.id %>
  
    <div class='chat-box ml-auto'>
      <p><%= chat.content %></p>
    </div>
  
  <% else %>
  
    <div class='chat-box mr-auto'>
      <p><%= chat.content %></p>
    </div>
  
  <% end %>

<% end %>